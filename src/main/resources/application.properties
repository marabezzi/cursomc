spring.profiles.active=${APP_PROFILE:dev}

default.sender=${APP_MAIL_SENDER:vazio}
default.recipient=${APP_MAIL_RECIPIENT:vazio}

jwt.secret=${JWT_SECRET:vazio}
jwt.expiration=${JWT_EXPIRATION:864000000}


aws.access_key_id=${AWS_KEY:vazio}
aws.secret_access_key=${AWS_SECRET:vazio}
s3.bucket=curso-spring-ionic-guilherme
s3.region=sa-east-1

img.prefix.client.profile=cp
img.profile.size=200
 
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

